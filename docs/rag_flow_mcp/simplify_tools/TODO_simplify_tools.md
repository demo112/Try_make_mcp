# 待办事项：RAG Flow MCP 工具精简优化

## 1. 短期优化
- [ ] **参数校验**: 为聚合工具 (`dataset_manage` 等) 添加更严格的参数校验逻辑，例如 `action` 为 `create` 时必须校验 `name` 参数的存在性（目前已有基础校验，可进一步增强）。
- [ ] **Schema 完善**: 在 docstring 中更详细地描述每个 action 对应的参数组合，帮助 LLM 更好地理解多态工具。

## 2. 长期规划
- [ ] **动态工具加载**: 考虑根据用户配置动态启用/禁用某些工具组（如 Governance 模块）。
- [ ] **工具分组**: 如果 FastMCP 支持，可以将工具在 UI 上进行视觉分组（目前 MCP 协议暂不支持，仅依赖命名约定）。
- [ ] **自动化测试**: 编写针对聚合工具的集成测试，确保每个 action 都能正确调用底层逻辑。

## 3. 已知问题
- 目前无重大已知缺陷。
